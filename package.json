{
  "name": "sam-suppliers",
  "version": "1.0.0",
  "description": "Supplier Reconciliation \u2014 SAM plugin (in development).",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cd frontend && npm install --no-audit --no-fund && npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.19.2",
    "knex": "^3.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "sqlite3": "^6.0.1",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "private": true
}
